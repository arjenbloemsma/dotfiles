# macOS specific configs (in dotfiles repo, stowed)
# Machine-local overrides go in .zshrc.local (not stowed)

# pnpm
export PNPM_HOME="~/Library/pnpm"
case ":$PATH:" in
  *":$PNPM_HOME:"*) ;;
  *) export PATH="$PNPM_HOME:$PATH" ;;
esac

export PATH="/opt/homebrew/opt/curl/bin:$PATH"
