# Catppuccin theme + fancy status bar.

set -g @plugin 'catppuccin/tmux'
set -g @catppuccin_flavor 'macchiato' # or frappe, latte, mocha

# Window styling — rounded pills
set -g @catppuccin_window_status_style "rounded"
set -g window-status-separator " "
set -g @catppuccin_window_number_position "right"
set -g @catppuccin_window_current_number_color "#{@thm_mauve}"
set -g @catppuccin_window_text "#W"
set -g @catppuccin_window_number "#I"
set -g @catppuccin_window_current_text "#W"
set -g @catppuccin_window_current_number "#I"

# Status line styling — rounded pills
set -g @catppuccin_status_left_separator  ""
set -g @catppuccin_status_middle_separator ""
set -g @catppuccin_status_right_separator ""
set -g @catppuccin_status_connect_separator "no"
set -g @catppuccin_status_fill "all"
set -g @catppuccin_directory_text " #{b:pane_current_path}"

# Status line content — references catppuccin's exported format strings.
# The #{E:...} expansion runs at render time, after the plugin has loaded.
set -g status-left ""
set -g status-right "#{E:@catppuccin_status_directory} #{E:@catppuccin_status_session} #{E:@catppuccin_status_date_time}"
set -g status-right-length 100
