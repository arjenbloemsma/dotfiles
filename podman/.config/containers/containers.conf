[engine]

# Suppress the "Executing external compose provider" banner that podman prints
# every time `podman compose` delegates to docker-compose. The banner has ANSI
# escapes that pollute structured output (e.g. devpod parses `compose ls
# --format json` and chokes on the escape codes).
compose_warning_logs = false
